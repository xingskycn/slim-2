// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("slim", "for slim support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("slim", "enable slim support", "no");

if (PHP_SLIM != "no") {
	EXTENSION("slim", "slim.c");
}

